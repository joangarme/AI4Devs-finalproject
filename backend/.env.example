# Personal Finance Tracker API - Environment Configuration
# Copy this file to .env and customize as needed
# The application will work with defaults if .env is not present

# ============================================================
# Application Settings
# ============================================================

# Application name displayed in API responses
APP_NAME=Personal Finance Tracker API

# Enable debug mode for development (true/false)
DEBUG=false

# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ============================================================
# Server Settings
# ============================================================

# Server host address (0.0.0.0 allows external connections)
HOST=0.0.0.0

# Server port number (1-65535)
PORT=8000

# API version 1 prefix for all endpoints
API_V1_PREFIX=/api/v1

# ============================================================
# CORS Settings
# ============================================================

# Allowed CORS origins (comma-separated)
# Use * for all origins (not recommended for production)
# Example: http://localhost:3000,https://yourdomain.com
CORS_ORIGINS=*

# Allow credentials in CORS requests (true/false)
CORS_ALLOW_CREDENTIALS=true

# Allowed HTTP methods for CORS (comma-separated or *)
CORS_ALLOW_METHODS=*

# Allowed headers for CORS (comma-separated or *)
CORS_ALLOW_HEADERS=*

# ============================================================
# Database Settings
# ============================================================

# Database connection URL
# 
# SQLite (default, recommended for development):
#   sqlite:///./app.db                    - Relative path (creates in backend/ directory)
#   sqlite:////absolute/path/to/app.db    - Absolute path (note: four slashes)
#
# PostgreSQL (recommended for production):
#   postgresql://user:password@host:port/database
#   postgresql+asyncpg://user:password@host:port/database  - Async driver
#
# MySQL:
#   mysql://user:password@host:port/database
#   mysql+pymysql://user:password@host:port/database      - Pure Python driver
#
# For development, the default SQLite path is usually sufficient
DATABASE_URL=sqlite:///./app.db

# Enable SQLAlchemy SQL query logging (true/false)
# Useful for debugging database operations in development
# Set to false in production to avoid performance overhead
DB_ECHO=false
